# `@clearideas/agent-runtime-artifacts`

Generate artifacts through any `SandboxProvider`. `ArtifactGenerator` stages
inputs and runtime files, executes a configured generator, collects bounded
outputs, validates formats, and terminates the sandbox.
