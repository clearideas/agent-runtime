# `@clearideas/agent-runtime`

Install the standard Agent Runtime composition:

```sh
npm install @clearideas/agent-runtime
```

The package exports the agent and agent run manifest contracts, execution core,
model and MCP configuration, standard step executors, local persistence, and
local or remote execution interfaces.

Provider-specific compute, sandbox, persistence, and telemetry adapters are
available as separate packages.
