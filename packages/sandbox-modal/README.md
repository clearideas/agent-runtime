# `@clearideas/agent-runtime-sandbox-modal`

Implements `SandboxProvider` for Modal. Applications provide
`ModalSandboxGateway`, the Modal SDK integration, and credentials. The provider
supports code steps and artifact generation.
