# @clearideas/agent-runtime-condition-jexl

Implements `ConditionEvaluator` with JEXL for `when`, loop conditions, and loop
goals.

Only evaluate manifests from trusted authors unless the host has reviewed the expression engine
and the variables exposed to it.
